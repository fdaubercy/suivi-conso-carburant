# Lessons Learned

| Date | Problème | Règle
|---|---|---|
| 2026-06-08 | Modules dupliqués après import .bas | Toujours vérifier VB_Name dans .bas avant import, utiliser set-module plutôt qu'import |
| 2026-06-14 | Erreur compilation VBAProject sur ligne Attribute VB_Name | Avant set-module, filtrer les lignes `^Attribute VB_Name` du .bas (métadata export uniquement, invalide comme code VBE) |
| 2026-06-08 | Ghost references dans le binaire VBA | Après suppression de module, sauvegarder et rouvrir le classeur avant toute exécution |
| 2026-06-08 | UTF-8 corruption dans commentaires VBA | Les fichiers .bas doivent être encodés en CP1252 (Windows-1252) pour Excel VBA |
| 2026-06-08 | 0x800ADF09 après modifications COM | Fermer/rouvrir Excel pour forcer la recompilation du p-code VBA |
| 2026-06-08 | ChrW(&H1F50D&) cause "Invalid procedure call" (emoji hors BMP, valeur > 65535) | ChrW() n'accepte que 0–65535 (BMP). Pour les emoji 4-octets (U+1F...), utiliser une alternative BMP ou supprimer le caractère |
| 2026-06-08 | Lignes `Attribute VB_Name = "..."` dans un .bas provoquent "Impossible d'exécuter la macro" | Ne jamais inclure de ligne `Attribute VB_Name` dans le code envoyé via set-module ou dans un .bas déployé. Cette directive est gérée par le VBE lui-même |
| 2026-06-08 | Tentative de recréer tasks/lessons.md alors qu'il existait déjà | Toujours vérifier l'existence d'un fichier (Read ou Glob) avant de le créer. L'instruction CLAUDE.md "créer s'il n'existe pas" s'applique seulement si le fichier est absent |
| 2026-06-08 | Règle CP1252 ci-dessus partiellement incorrecte : vba_agent.py lit les .bas avec encoding="utf-8-sig" | Les .bas sur disque peuvent rester en UTF-8. La corruption survient si on crée un fichier temp CP1252 depuis un .bas UTF-8 sans conversion correcte des caractères multi-octets |
| 2026-06-08 | Attribut VB_Name dans les .bas disque provoque "Impossible d'exécuter la macro" quand déployé via set-module | Toujours filtrer la ligne `Attribute VB_Name` avant d'appeler set-module (garder VB_Name dans le .bas disque pour la documentation, le retirer dans le code envoyé au VBE) |
| 2026-06-08 | **TEXTES GARBLÉS (Ã‰ au lieu de É, â€" au lieu de —, â‚¬ au lieu de €) sur les shapes du dashboard — CAUSE RACINE DÉFINITIVE** | Ne JAMAIS convertir un .bas UTF-8 en fichier temp CP1252 avant déploiement. `vba_agent.py` lit déjà en utf-8-sig : déployer **directement** depuis le .bas UTF-8 du disque, en retirant uniquement la ligne `Attribute VB_Name`. La conversion CP1252 corrompt les caractères multi-octets LITTÉRAUX (É=0xC3 0x89, —=U+2014, €=U+20AC) présents dans les chaînes de code (ex. SetText, AddKpiCard de modDashboardGraphiques). NB : modSidebar utilise `ChrW()` pour ses accents → source ASCII pure → la conversion CP1252 y était inoffensive, d'où la confusion. **Règle : 1 seule procédure de déploiement = strip VB_Name → set-module depuis l'UTF-8, sans temp CP1252.** Après déploiement, relancer la macro de rendu (MAJ_Dashboard_Graphiques) pour réécrire les shapes. |
| 2026-06-08 | VBE affiche un buffer de code PÉRIMÉ après set-module (volet ouvert montre l'ancien code, ex. AutoNavigatePreview alors que le module déployé a AutoCollapsePreview) → "Erreur de compilation : Variable non définie" au clic sur une shape | Le set-module COM met bien à jour le CodeModule (vérifiable par inspect), mais un volet de code ouvert dans le VBE peut garder un buffer en cache et compiler à partir de lui. Fix : `save` puis fermer/rouvrir le classeur via COM (wb.Close(False) + Workbooks.Open) pour purger le buffer et forcer une recompilation propre. Vérifier ensuite via COM que le code rechargé est bien la nouvelle version. |
| 2026-06-08 | Shape-bouton (fup_btn) doit recouvrir exactement une cellule (B6) à tout niveau de zoom | Range.Left/Top/Width/Height renvoient des points (coordonnées document, invariantes au zoom). Une AddShape utilise les mêmes points. Donc `shape.Width = b6.Width` (sans marge) garantit le recouvrement exact à 100% comme à 60%. Ne pas ajouter de marge type `+30`. |

| 2026-06-09 | -2146788248 au clic Reglages/Suivi Carburant : conclu a tort "artefact COM" puis "le module compile" (j ai teste ReglageSeparateurCSV qui n utilise PAS la const fautive) | Une Private Const mal placee (apres une procedure) ne casse QUE les procedures qui REFERENCENT la const ("Variable non definie" sur son nom, en compile-on-demand), pas tout le module. Tester une fonction qui n utilise pas la const donne un faux "OK". Tester une fonction qui l utilise, ou demander a l utilisateur Debogage->Compiler VBAProject (pointe ligne+symbole exacts). |
| 2026-06-09 | Call SyncStationsVersGoogleSheets orphelin (module synchroniseGoogleForm supprime) -> -2146788248 au Worksheet_Activate de Suivi Carburant | Un Call X vers une procedure d un module SUPPRIME = erreur de COMPILATION "Sub or Function not defined", NON rattrapable par On Error Resume Next. Apres suppression d un module, grep tous les Call/OnAction/OnTime/Run vers ses procedures. |
| 2026-06-09 | Distinguer compile vs runtime via COM (Application.Run) | -2146788248 avec description VIDE (None) = erreur de COMPILATION ; avec description renseignee = erreur d EXECUTION. Discriminant fiable. Ne jamais classer une erreur COM reproductible en "artefact" sans isoler (events OFF, appel direct du helper). |
| 2026-06-09 | Boutons-image (AddPicture/AddShape) derivaient de +23 pt vers la droite quand StyleParamsPanel elargit les colonnes A:B | Toute shape/bouton-image positionnee en coordonnees ABSOLUES doit recevoir `.Placement = xlFreeFloating` (sinon defaut xlMoveAndSize -> bouge avec les cellules). La sidebar le faisait deja ; EnsurePictureButton (modGraphiques) et AddButton (modDashboardGraphiques) non -> corrige. |
| 2026-06-10 | Plan X39 figeait "Chronologie non scriptable (Add2 xlTimeline -> Err 5 en VBA) => handoff manuel". FAUX en COM. | Une **Chronologie (timeline) EST creable par COM/pywin32 late-binding** : `wb.SlicerCaches.Add2(pivot, "Date", "nom", 2)` cree le **cache** (SlicerCacheType=2), PUIS `cache.Slicers.Add(feuille, Name:=.., Caption:=.., Top, Left, Width, Height)` cree le **controle visible** (sans le 2e appel, pas de shape et TimelineState erre). Passer les args en **mots-cles** (un `Level=""` positionnel donne E_INVALIDARG 0x80070057). Pas besoin de handoff utilisateur. |
| 2026-06-10 | `SlicerCaches("tl").TimelineState.StartDate/EndDate` leve une exception en **Python COM** (artefact late-binding) mais se lit **sans erreur en VBA** | Pour lire l'etat d'une chronologie, le faire **en VBA** (`TimelineState.StartDate/EndDate`, `SetFilterDateRange d1,d2`), pas via pywin32. Ne pas conclure "API cassee" depuis une erreur COM Python : tester l'equivalent VBA (injecter un Sub diag + run). Un changement de chronologie declenche bien `Workbook_SheetPivotTableUpdate` (comme un segment). |
| 2026-06-10 | `Get-Content $f \| Measure-Object -Line` a renvoye 1431 au lieu de 1543 (fausse alerte "miroir disque perime") | Pour compter les lignes d'un fichier en PowerShell, utiliser `(Get-Content $f).Count`, PAS `Get-Content \| Measure-Object -Line` (qui compte les sauts de ligne DANS chaque element deja splitte = souvent 0). Verifier un ecart de comptage avant de conclure a une desynchronisation. |
| 2026-06-10 | Etiquette au survol des boutons via hyperlien+ScreenTip : KO total (aucune etiquette ET clic mort). Sur des shapes-image, l'hyperlien INTERCEPTE le clic (OnAction ignore) et `Worksheet_FollowHyperlink` NE se declenche PAS pour les shapes → bouton inerte. | Pour un tooltip au survol sur un bouton-image Excel : l'astuce hyperlien+ScreenTip ne marche PAS ici (casse le clic, ScreenTip non affiche). Seul l'ActiveX (`Forms.Image`/`CommandButton` + `ControlTipText` + `_Click`) donne survol+clic, mais instable + `LoadPicture` ne lit pas les PNG. Si tooltip demande : tester ActiveX en UI AVANT de livrer, sinon garder OnAction seul (sans tooltip). Toujours faire tester le clic/survol REEL par l'utilisateur (non testable en COM). |
| 2026-06-10 | Date du dernier plein affichee 06/09/2026 (6 sept) au lieu de 09/06/2026 (9 juin) dans les graphiques. `ImporterNouveauxPleins` faisait `ParseGoogleDate(CStr(cell.value))` sur une cellule `_ImportGS!Date` qui contient une VRAIE date. `CStr` → "09/06/2026" (locale FR) ; `ParseGoogleDate`, croyant recevoir du M/J/A US, lit jour<=12 comme un mois → 6 sept. Seules les NOUVELLES lignes (non encore dedupliquees) etaient touchees. | Ne JAMAIS `CStr()` une vraie date pour la re-parser : CStr est locale-dependant. Lire la date reelle : `If IsDate(cell.value) Then CDate(cell.value)` (le serial est locale-independant). L'heuristique « 1er nombre <= 12 ⇒ mois (US) » est FAUSSE pour des dates FR jj/mm/aaaa (jour <= 12). Pour ecrire une date via pywin32 sans decalage de fuseau (-2h), utiliser le **serial Excel** (`cell.Value2 = (date - 1899-12-30).days`), pas un `datetime` naif. |
| 2026-06-11 | `TimelineState.SetFilterDateRange d1, d2` sans effet (no-op silencieux) quand une borne porte une HEURE (dernier plein a 02:00) — le calage par defaut de la chronologie (`ApplyDefaultPeriodOnce`) ne « tenait » pas. | Pour caler une chronologie sur une plage, **arrondir les bornes au jour plein** (`Int(serial)`) avant `SetFilterDateRange` : une borne fractionnaire (date+heure) est ignoree. Enrober en plus la sequence de calage de `Application.EnableEvents = False` : sinon `RecreerDashboardComplet` declenche `Workbook_SheetPivotTableUpdate` → `ApplyFiltersFromControls` qui **re-applique l'ancien filtre** et annule le calage. Isoler via un helper qui lit l'etat SANS rebuild pour distinguer « SetFilterDateRange ne colle pas » de « le rebuild reverte ». |
| 2026-06-11 | Fichiers vides parasites a la racine (`code`, `$null`, `corrige`, `ui-coder`, `reconstruire`...) ramasses par `git add -A` (commit.sh) et parfois committes. `code` venait du **message de commit** contenant `design->code` : le `>code` interprete comme **redirection shell** par un hook PowerShell (PostToolUse) cree un fichier vide `code`. `$null` vient des hooks `git ... 2>$null` mal contextualises. | NE JAMAIS mettre de caracteres de redirection shell (`>`, `<`, `\|`, `&`, `2>`) dans un message de commit ou une description : ecrire « design vers code », pas « design->code ». Gitignorer les noms parasites connus. Avant `commit.sh`, faire un sweep des fichiers 0-octet a la racine (`git ls-files --others --exclude-standard` + test `! -s`) et les supprimer. Cause cote hooks : `.claude/settings.json` (Stop + PostToolUse) utilise `2>$null`/redirections — a durcir si recurrence. |
| 2026-06-12 | Bouton de rayon sélectionné (`.radius-btn.active`) illisible en thème sombre : l'override `[data-theme="dark"]` ne posait que `color:#0f172a` (texte sombre) sans overrider le `background` (resté `var(--blue-dark)` = `#0f1e30`, sombre) → texte sombre sur fond sombre, contraste **1.06:1**. | Quand on ajoute un override dark-theme `color:` sur un état actif/sélectionné, TOUJOURS overrider aussi le `background` (un texte sombre n'est lisible que sur fond clair). Vérifier le contraste WCAG des DEUX thèmes (cible ≥ 4.5:1). S'aligner sur le patron existant `.submit-btn` (fond `--blue-mid` vif + texte `#0f172a` → 7.02:1). |
| 2026-06-14 | Boutons navbar (`modSidebar`) : libellés longs (« Tableau de bord », « Réglages » actif/gras) tronqués/masqués — largeur fixe 108pt + `WordWrap=True` → le texte passait à la ligne hors du bouton (24pt de haut) → invisible. | Boutons-shapes à libellé variable : `TextFrame2.WordWrap=msoFalse` + `AutoSize=msoAutoSizeShapeToFitText`, **mesurer en GRAS** (cas le plus large : l'onglet actif met le texte en gras, sinon il déborde au survol/clic), puis repositionner selon `nav.Width` réel. **Ajouter ~24pt visuels de marge** (`+24/z`) : Excel MESURE les emojis plus étroits qu'il ne les AFFICHE (🏠 maison, 🗺 carte surtout) → sans marge, troncature de 2-3 caractères. Vérifier par export image : `Range.CopyPicture(xlScreen,xlBitmap)` → `ChartObjects.Add` → `Chart.Paste` → `Chart.Export(png)`. |
| 2026-06-14 | "Erreur à l'ouverture Excel" supposée venir du classeur projet (Workbook_Open) alors qu'elle venait d'une `Copie de AutomateExcel_AddIn.xlsm` parasite dans **XLSTART** (contenu add-in `.xlam` renommé `.xlsm` → "format ou extension non valide"). J'ai longuement instrumenté Workbook_Open pour rien avant de demander le texte exact. | Pour toute "erreur à l'ouverture Excel" : **obtenir d'abord le TEXTE EXACT du dialogue** (souvent il nomme le fichier fautif). Vérifier le dossier d'auto-démarrage `%APPDATA%\Microsoft\Excel\XLSTART` + les compléments chargés via registre (`HKCU\...\Excel\Options\OPEN=`) et `AltStartup` AVANT de suspecter le `Workbook_Open` du projet. Ne pas supposer que l'erreur concerne le fichier sur lequel on travaille. Correctif type : sortir le fichier parasite de XLSTART (move réversible), garder l'original `.xlam`. |
| 2026-06-14 | -2146788248 au clic selecteur vehicule : EnsureHeaderBand ne compilait pas. Conclu a tort "corps propre" en lisant seulement les Dim (toutes declarees) — erreur reelle = "Type argument ByRef incompatible" : `hdrTop As Single` (issu de NavbarBottom() As Single) passe ByRef a `EnsureShape(... T As Double)` ; idem `btnTop`->`EnsurePictureButton`. Introduit par le refactor Top-litteral->NavbarBottom() : un litteral 6/82 passait ByVal (OK), une VARIABLE Single passee ByRef a un param Double = erreur de COMPILATION. | En VBA, un argument passe ByRef exige une correspondance de type EXACTE : une variable `As Single` -> parametre `As Double` leve "Type argument ByRef incompatible" a la COMPILATION (compile-on-demand : ne surgit qu'a l'execution de la 1re ligne appelant la proc, jamais rattrapable par On Error). Lire les Dim ne suffit PAS : croiser le TYPE de chaque variable-argument avec la SIGNATURE du parametre. Fix : aligner le type (declarer la variable comme le param) ou forcer ByVal via parentheses `f((x))`. Localisation : Debogage->Compiler VBAProject EN PREMIER (pointe ligne+symbole, lecon #19) ; sinon tracage par checkpoints fichier (DiagStep ecrit dans %TEMP%) qui isole un compile-on-demand fault en 1 seule execution. |
| 2026-06-14 | Retrait d'un add-in charge par XLSTART (AutomateExcel_AddIn.xlam) : Move-Item echoue ("used by another process") car Excel le tient ouvert. L'add-in n'apparait PAS dans l'iteration Application.Workbooks ni dans AddIns/AddIns2 (Installed=False en chargement XLSTART) -> on croit a tort qu'il n'est pas charge. | Pour retirer un add-in XLSTART SANS fermer Excel : y acceder PAR NOM via xl.Workbooks("Nom.xlam").Close(SaveChanges:=False) (accessible meme s'il est exclu de l'iteration) pour liberer le verrou, PUIS deplacer le fichier hors de XLSTART (reversible vers Documents\Sauvegardes-XLSTART). NE JAMAIS Stop-Process EXCEL si le classeur projet est ouvert (risque de perte de donnees). Verifier les dependances AVANT tout retrait : Application.Run (refs molles), VBProject.References (refs dures), UserForms (controle date-picker) -- Suivi Conso n'utilise AUCUN des 7 add-ins XLSTART. Note : une instance Excel lancee par COM/automation (vba-agent) NE charge PAS XLSTART ; des locks ~$ horodates a la meme minute sont des orphelins. |
| 2026-06-14 | Reprise depuis `session-export-suivie85.zip` : le NOM de l'export ("e85") ne refletait PAS le projet actif (le fil reel = `suivi-conso-carburant`, classeur Excel). En plus, le script diagnostic interrompu (`Downloads\reprise-extract\check_dashboard.py`) avait ete nettoye entre sessions (dossier absent). | Pour une reprise : identifier le projet actif via les DERNIERS tours user/assistant du transcript JSONL (pas le nom du .zip). Les scripts temp de `Downloads\reprise-extract\` ne persistent pas d'une session a l'autre -> les RECREER depuis l'input `tool_use` du transcript. Toujours verifier l'etat REEL (git status, shapes/charts via COM, source .bas) avant de classer une tache "finalisee" : ici hdrBand+10 charts presents en live ET `hdrTop/btnTop As Double` en source = double preuve du fix ByRef. |
| 2026-06-15 | Reprise « commit le vba » : lancer `commit.sh` est BLOQUE par le classifieur auto-mode car `commit.sh` couple commit ET `git push origin main` (branche par defaut) — push non explicitement autorise (« commit » != « push », et la question de perimetre etait restee sans reponse). | « commit le X » = COMMITTER seulement, JAMAIS pousser sans accord explicite. Pour respecter le gate projet sans pousser : reproduire `commit.sh` a la main = aligner `package.json` (version source = `js/config.js`), `git add -A`, `git commit` (le hook husky lint-staged = eslint + vitest related sert de gate au commit), puis STOP avant `pull --rebase`/`push`. Pousser ensuite SUR ACCORD = `git push origin main` simple (re-lancer `commit.sh` sur un arbre propre sort au step 2 « aucun changement » sans rien pousser). Hygiene : separer le travail DEMANDE (feat VBA) des changements transverses traines dans l'arbre (fusion `.claude/CLAUDE.md`->`CLAUDE.md`, tooling) en commits distincts par staging selectif AVANT `commit.sh` (qui fait `git add -A` = tout). |
| 2026-06-15 | `graphify-out/graph.json` ampute de moitie (2235 -> 1092 noeuds) par un auto-`--update` lance entre deux sessions, puis committe sans verification (dans le commit VBA) : un update code-only SANS cle LLM (GEMINI/GOOGLE) ne regenere QUE les noeuds AST et ecrase les noeuds semantiques/docs. graph.html embarque son propre snapshot (n'est pas un lecteur live de graph.json) -> il affichait encore l'ancien compte. | Apres tout `graphify --update`/auto-update, VERIFIER le compte de noeuds avant de committer graph.json : `(Get-Content graphify-out/graph.json -Raw \| ConvertFrom-Json).nodes.Count` vs la version precedente (`git show HEAD:graphify-out/graph.json`). Une chute brutale = amputation. Restaurer la structure code par `graphify update . --force` (re-extraction AST des fichiers, sans LLM ~2033 noeuds) ; les ~200 noeuds semantiques restants exigent une cle LLM. `--force` ecrase MEME si moins de noeuds -> ne l'utiliser qu'avec le graph.json courant deja committe (backup git). `cluster-only` re-clusterise (gros diff graph.json) ; `update --force` re-extrait (recommande pour reparer une amputation). |
| 2026-06-16 | KPI du dashboard figes a 0 alors que les donnees existent : conclu trop vite "pas de rebuild a l'ouverture" puis "desync segments". VRAIE cause = en-tetes de la table GS_Pleins corrompus (Km/Litres/PrixL/Date renommes '0'/'2'/'3'/date) -> ColIdx("Km")=0 -> ComputeDashboardStats `If ciKm=0 Then Exit Sub` -> 0 partout. | Pour un agregat/KPI a 0 alors que les donnees existent : verifier d'ABORD le mapping colonnes (ColIdx + en-tetes de la ListObject source) AVANT de suspecter le filtre ou le declencheur de rebuild. Un `If ciX=0 Then Exit Sub` silencieux sur un header renomme donne 0 partout. Source autoritaire des bons noms = la table d'import (_ImportGS). Restaurer via `ListColumns(i).Name` (donnees intactes, reversible) et VERIFIER par un rebuild (KPI passent de 0 a des valeurs reelles). |
| 2026-06-16 | `Feuil4.Worksheet_Change` (GS_Pleins) estampillait `Modifie_local = Now()` DANS la ligne d'en-tete de la table (col 17 = timestamp). Garde-fou `Intersect(..., Rows("2:..."))` excluait la ligne 1, mais la table a son header en LIGNE 2 ($A$2:$Q$17). | Un garde-fou "ignorer l'en-tete" code en dur (`Rows("2:...")`) casse si la table n'a pas son header en ligne 1. TOUJOURS deriver la ligne d'en-tete de la table : `ws.ListObjects(1).HeaderRowRange.Row`. Sinon toute ecriture programmatique sur le header (ex. renommage de colonnes) re-declenche la corruption (le renommage lui-meme l'a re-stampe). |
| 2026-06-16 | Alerte "miroir disque vba/ STALE" (hash + nb lignes != live sur TOUS les modules) = FAUSSE alerte : diff 100% cosmetique (ligne `Attribute VB_Name` presente/absente selon fichiers, normalisation de casse par le VBE `Sh`->`sh`/`Now`->`now`/`Nz`->`nZ`, encodage UTF-8 disque vs CP1252 export COM). | Avant de conclure a une derive de code, DIFFER (`git diff --no-index disque live`) : un hash different peut n'etre que casse/encodage/Attribute. Pour editer sans risque de divergence : travailler le CodeModule LIVE (`cm.Lines`, unicode) via remplacements de sous-chaines ancres sur des LITTERAUX (stables au re-casing VBE) et non sur des identifiants. |
| 2026-06-16 | Navbar (`PoserSidebarSurFeuille`) : `x = x + wReal + gap` produisait quand meme des chevauchements en live (interaction largeur AutoSize/bold/zoom non elucidee). | Pour une rangee de shapes a largeur VARIABLE garantie sans chevauchement : faire une 2e PASSE apres creation qui repositionne chaque shape selon sa `.Width` FINALE reelle (`xx = padL: For each: nv.Left = xx: xx = xx + nv.Width + gap`). Ne pas se fier a la largeur calculee pendant la pose initiale. |
| 2026-06-16 | Boutons icone+libelle a caser dans un bandeau decale/retreci (titre+sous-titre centres occupent le milieu) : `NavbarBottom+80` fixe -> debordent sous le bandeau sur les segments. | Ancrer la rangee de boutons sur le BAS du bandeau (`ht + hH - 40`, label `ht + hH - 13`), pas sur un offset fixe depuis la navbar. Les poser en bas a GAUCHE (zone visuellement vide car titre centre). Coherence inter-modules : dash_btn (modDashboardGraphiques) et btnRecreer/Export (modGraphiques) doivent utiliser la MEME formule Y (`ws.Range("A7").Top - 42`). |
| 2026-06-16 | `excel/Suivi Conso Carburants.xlsm` (pourtant suivi par git) n'apparaissait PAS dans `git status` malgre la sauvegarde -> impossible a committer. Cause = flag git `assume-unchanged` (visible en minuscule `h` dans `git ls-files -v`, vs `H` pour les fichiers normaux). | Si un fichier SUIVI ne montre jamais ses modifications dans `git status` : verifier `git ls-files -v <file>` (lettre minuscule = assume-unchanged, `S` = skip-worktree). Lever avec `git update-index --no-assume-unchanged <file>`. Classique sur un gros binaire (.xlsm) qu'on ne veut pas committer a chaque save. Le classeur live ouvert ETAIT le fichier git-suivi (`wb.FullName`) -> `wb.Save()` suffit a le rendre committable une fois le flag leve. |
| 2026-06-16 | `/graphify --update` AMPUTE le graphe meme via le flow interactif `build_merge` (2033 -> 1685 : dedup de 348 noeuds), pas seulement via l'auto-update headless (lecon #37). La securite interne de graphify a refuse d'ecraser (« Refusing to overwrite: new graph has 1685 nodes but existing has 2033 »), MAIS un `to_json()` manuel apres coup a quand meme ecrit la version amputee. | Ne pas faire confiance a `build_merge` pour un update : il dedup agressivement. Avant tout `graphify --update` pre-commit : `cp graph.json .backup`, noter le compte de noeuds, et APRES verifier le compte ; si chute, `git checkout HEAD -- graphify-out/` (restaure graph.json + GRAPH_REPORT + html + manifest d'un coup). Ne JAMAIS appeler `to_json()` directement apres un `build_merge` qui a affiche « Refusing to overwrite ». graphify ne reconnait pas le VBA (.bas/.cls, obs #779) -> les modifs VBA ne sont de toute facon jamais dans le graphe ; le gate pre-commit n'apporte rien pour un commit VBA-only et ne doit pas committer un graphe ampute. |
| 2026-06-17 | Chirurgie OOXML retirant un externalLink (samradapps_datepicker) du .xlsm : l'avertissement "mettre a jour les liaisons" a disparu MAIS Excel a leve "probleme dans le contenu de ... recuperer le maximum" au re-open (regression signalee par l'utilisateur, capture L208). Cause = `xl/drawings/drawing6.xml` referencait le lien via `[1]` (reste de controle date-picker OLE) -> en retirant l'externalLink sans nettoyer le drawing, le dessin pointait dans le vide. Excel a ensuite "recupere" tout seul (drawing6 reecrit 7763->12308 o, `[1]` supprime) = fix non maitrise, jamais commite. | Avant de retirer un externalLink par edition OOXML, GREPPER `[N]` (N = index du lien, ex. `[1]`) dans TOUS les `xl/drawings/drawing*.xml` + leurs `_rels` ET les formules de feuilles. Si un drawing/OLE/nom defini le reference, retirer AUSSI ce controle dans la meme chirurgie (sinon corruption "probleme de contenu"). Un lien "sans formule" (LinkSources vide) n'est PAS forcement "fantome" : un controle OLE peut le pointer. Toujours faire CONFIRMER le re-open reel par l'utilisateur (texte exact du dialogue, lecon #32) AVANT de classer #fait, et committer le resultat (ne pas laisser Excel "recuperer" silencieusement un .xlsm non versionne). |
| 2026-06-17 | Déploiement VBA via script COM ad-hoc (`GetActiveObject` + `cm.DeleteLines(1,N)` puis `cm.AddFromString` sur un module ~500 l.) : HANG transitoire en pleine écriture → `modWorkbook` laissé VIDE (DeleteLines exécuté, AddFromString bloqué, process tué à ~200 s). Au 2e essai via `vba_agent set-module` (MÊME mécanisme COM) : succès → le hang était transitoire. | Écriture d'un CodeModule en COM : (1) TOUJOURS borner par timeout (Start-Job/Wait-Job) ; (2) après écriture VÉRIFIER que le module n'est pas vide (`inspect` lines>0) ; (3) préférer `vba_agent set-module/set-doc` (éprouvé) à un script `GetActiveObject` maison ; (4) `DeleteLines`+`AddFromString` n'est PAS atomique → si l'écriture hang le module reste vidé : ne JAMAIS `wb.Save` avant d'avoir re-vérifié, garder un `backup`, et le .xlsm disque non sauvé = filet (close sans save = retour disque). Encodage : `comp.Export()` écrit en CP1252 (pas UTF-8) → relire en cp1252 puis réécrire UTF-8 (direction sûre, mono-octet). Fichiers temp en mode BINAIRE (`open(...,"wb")` + CRLF explicite) : le mode texte Windows retraduit \n→\r\n et double les CRLF (\r\r\n = lignes vides fantômes). |
| 2026-06-18 | Reprise flexfuel : en câblant la MAJ globale post-plein (D1), `window.dispatchEvent(new CustomEvent('plein-added'))` a fait échouer le lint (`'CustomEvent' is not defined`, `no-undef`) car la config ESLint du projet (`eslint.config.js`) ne déclare en globals que `window`/`document`, pas les constructeurs DOM `CustomEvent`/`Event`. | Dans ce projet, dispatcher un événement custom via **`new window.CustomEvent('x', { detail })`** (convention déjà en place : `auth.js`, `router.js`, `parametres.js`, `vehicules.js`), jamais `new CustomEvent(...)` nu. Idem pour tout constructeur DOM non listé dans les globals ESLint : le préfixer par `window.`. Lancer `npx eslint <fichiers touchés>` avant de considérer une étape JS terminée (gate `--max-warnings=0`). |
| 2026-06-18 | X39 (ROADMAP) décrivait « `RefreshPrix` écrase `Prix par Station`, créer `_PrixHistoriqueJournalier` » : **périmé**. Le GAS accumulait déjà par append dans `_PrixHistory` (6 carburants) et la PQ `PrixHistory.m` le miroitait. Le VRAI bug était ailleurs : `modGraphiques.BuildPriceBlockMerged` SOURCE 2 lisait la table `PrixHistory` (format LONG Station/Date/Type/Prix) en cherchant des colonnes LARGES « E85 station »/… inexistantes → n'alimentait jamais `gPrice`. | Avant d'implémenter un item ROADMAP/spec, **vérifier l'état réel du code live** (les specs périment au fil des refactos). Pour une donnée « qui n'apparaît pas dans un graphe », tracer la chaîne complète source→lecture (ici : `_PrixHistory` GAS → `PrixHistory.m` PQ → `BuildPriceBlockMerged` VBA) et confirmer le **format réel** de la table (long vs large) avant de coder. Surfacer au lieu d'implémenter aveuglément un spec contredit par le code. |
| 2026-06-18 | Déploiement VBA via `vba-agent` : `set-module`/`inspect` (accès VBE COM) OK, mais `run --macro` échouait pour TOUTES les macros (mêmes celles non touchées) avec `-2146827284` « impossible d'exécuter la macro… macros désactivées ». Faux diagnostic tentant : « mon edit casse la compilation ». | Quand `Application.Run` échoue pour TOUTES les macros alors que `inspect`/`set-module` marchent, c'est **environnemental** (macros désactivées cette session = bandeau « Activer le contenu » non cliqué, ou Excel non-idle/cellule en édition), PAS forcément ton code. **Disambiguer par un test de revert** : redéployer le module ORIGINAL (backup) et relancer une macro pré-existante ; si elle échoue encore → environnement (ton code est bon, redéployer). La compilation ne peut alors pas être vérifiée par COM : déployer + vérifier le contenu (re-`inspect` + diff vs miroir), `save`, et déléguer la vérif visuelle à l'utilisateur (« Activer le contenu » puis bouton de reconstruction). |
| 2026-06-20 | Reprise « essencee85 » : `-2146827284` sur TOUTES les macros (cf. leçon #49 « environnemental ») avait en fait une cause RACINE de code : `vba_agent set-module` injecte la ligne d'en-tête `Attribute VB_Name = "..."` du `.bas` **dans le corps du module** (il ne la retire pas) → le projet ne compile plus → blocage de toutes les macros. | Avant de classer un `-2146827284`-toutes-macros en « environnemental », **scanner chaque CodeModule pour une ligne `Attribute ` parasite en tête** (`cm.Lines(1,1).strip().startswith("Attribute ")`) et la retirer (`DeleteLines`), puis re-tester un `run` réel. Toujours **stripper la ligne `Attribute VB_Name`** du code envoyé à `set-module` (leçons #11/#14) — `vba_agent` ne le fait pas. Parasite retiré + `save` + un `run` qui réussit = preuve que le projet compile à nouveau. |
| 2026-06-20 | Graphe `gPrice` sans marché (GAZOLE/SP95 vides) + « plus de relevés après le 16/06 » : la requête Power Query **`PrixHistory` du classeur était une COPIE de `GS_Pleins`** (lisait l'onglet `_ImportGS` des pleins, 16 col) au lieu des relevés marché `_PrixHistory` (4 col long Station/Date/Type/Prix). Le fichier disque `powerquery/PrixHistory.m` était pourtant correct → **live ≠ disque**. | Ne JAMAIS supposer que la requête PQ live == le `.m` disque. **Vérifier le schéma de sortie réel** (`ListObject` : noms + nb de colonnes via COM) et au besoin le code M live (`wb.Queries(nom).Formula`). Re-synchroniser depuis le disque : `wb.Queries(nom).Formula = open("powerquery/Nom.m", encoding="utf-8-sig").read()` puis `lo.QueryTable.Refresh(False)`. Contrôler le compte lignes/colonnes après (ici 16-col pleins → 4-col marché, 3922 lignes, dates à jour). |
| 2026-06-20 | En-têtes de la requête `GS_Pleins` corrompus (`Date`→`01/01/1970`, `Km`→`0`, `SP98 station`→`Colonne2`…) par une ligne « fantôme » (echo d'en-tête `sync_id="sync_id"`) entrée dans le GS `_ImportGS` le 17/06. La col K `Tableau2` (« Prix S98 jour ») dépend de `SP98 station` → cassée pour les pleins récents. | Corruption d'en-têtes (type leçon #38) = **ligne fantôme à la source** : la **supprimer dans le Google Sheet** (Sheets API `batchUpdate deleteDimension`, critère sûr : ligne de données dont col O `sync_id` == "sync_id") PUIS un simple `QueryTable.Refresh(False)` restaure les noms de colonnes. Vérifier les `ListColumns` après. Garder les gardes anti-réapparition : GAS `handleExport` filtre `sync_id==="sync_id"` + Excel `IsGarbageSid` (import + re-push). |
| 2026-06-20 | Suppression de la ligne fantôme via Sheets API : `403 PERMISSION_DENIED / SERVICE_DISABLED` alors que le `refresh_token` portait bien le scope `spreadsheets`. | **Scope OAuth accordé ≠ API activée** dans le projet Cloud. L'API **Google Sheets** doit être activée séparément (`console.developers.google.com/apis/api/sheets.googleapis.com`, projet du `client_id`) — l'API Apps Script l'était déjà (déploiement OK mais écriture Sheets KO). Le message d'erreur 403 donne l'`activationUrl` exacte : la transmettre à l'utilisateur (1 clic). |
| 2026-06-21 | Reprise « e85 » (= reprise de l'export « diesel ») : `npx vitest run` complet renvoie **exit 1** avec 18 « Unhandled Error » `Failed to start forks worker` / `Timeout waiting for worker to respond` (durée anormale ~434 s, machine chargée + Excel ouvert) ALORS QUE **93/93 tests PASSENT**. La session « e85 » s'était bloquée puis tuée (limite de session) en boucle sur ce run de tests sans jamais committer le fix carte déjà prêt. | Un `vitest` exit 1 dont la sortie ne montre que des **timeouts de démarrage de workers** (pool `forks`) et **0 test en échec** (« Tests N passed (N) ») est **environnemental**, pas un échec de code (cf. #49/#50). Ne PAS boucler dessus : (1) lire le bilan « Tests X passed » comme source de vérité ; (2) reconfirmer vite via `vitest related --run js/<fichiers> --no-file-parallelism` (sous-ensemble séquentiel, évite la tempête de forks) ; (3) le hook husky `vitest related` ne portant que sur les fichiers stagés, le commit passe quand même. Ne jamais relancer la suite complète en boucle pour « voir si ça passe ». |
| 2026-06-23 | Reprise « fuel » (export `session-export-fuel.zip`, session `99c4607c`, opus/xhigh/ultracode) : la session précédente a **brûlé tout son budget en exploration lecture seule** (Read/Glob/Grep/PowerShell + 1 sous-agent `Explore`) et a heurté la **limite de session** AVANT le moindre `Edit`/`Write`/`vba_agent` → **0/5 demandes traitées** (1 diagnostic partiel, 4 à faire). Les `Glob **` partis de `C:\Users\fdaub` ont aussi timeout (scan node_modules + tout le profil). | Pour statuer « traité ou non » sur un export, **3 preuves convergentes** : (1) `git status --short` propre = aucun travail en cours ; (2) scanner le transcript pour des `tool_use` d'écriture (`Edit`/`Write`/`vba_agent`) — **que des outils lecture seule = 0 livrable** ; (3) `git log --date=iso` : aucun commit dans la fenêtre horaire de la session = rien commité. Récupérer les diagnostics utiles des **sous-agents** de l'export (jsonl `subagents/`), ils ne sont pas dans le contexte perdu. **Budget** : en session lourde (opus/xhigh), ne pas tout consacrer au repérage — cibler vite (PowerShell `Get-ChildItem` au lieu de `Glob **` qui scanne node_modules/profil) et basculer en édition tôt pour ne pas mourir en phase d'exploration. |
| 2026-06-23 | Reprise « fuel2 » (= reprise de la reprise « fuel », session `94960b7e`) : fuel2 avait CODÉ le fix #1 (historique 01/01/1970) + bump version + CHANGELOG/ROADMAP/tests mais est MORTE en plein `/graphify --update` → **tout NON COMMITÉ** dans le working tree (exactement la leçon #56). En prime, fuel2 avait FUSIONNÉ par erreur 2 sections du CHANGELOG (5.21.2.0 renommée en 5.21.3.0, texte carte recollé). | Sur reprise, vérifier `git status`/`git diff` AVANT tout : un fix « déjà codé mais non commité » se **finalise** (lint + `vitest related --run --no-file-parallelism`, réparer les docs, committer) au lieu d'être refait. Toujours relire `git diff CHANGELOG.md` après une session interrompue : un mauvais merge peut fusionner/écraser une section de version → restaurer via `git show HEAD:CHANGELOG.md`. |
| 2026-06-23 | Excel 365 via COM : réécrire une formule MATRICIELLE (`AVERAGE(IF((cond)*(cond),val))`) avec `.Formula` la CASSE (résultat ""/scalaire via IFERROR) car `.Formula` applique l'intersection implicite `@` ; et `.FormulaArray` ÉCHOUE (« Impossible de définir FormulaArray ») avec des références structurées `Tableau2[...]`. | Pour (ré)écrire une formule array à structured refs en Excel 365 : utiliser **`Range.Formula2`** (préserve les tableaux dynamiques, pas de `@`). `.Formula` ne convient qu'aux formules scalaires. Après écriture, VÉRIFIER la valeur (une formule cassée renvoie "" silencieusement). Sauvegarder un backup JSON de toutes les `cell.Formula` AVANT toute chirurgie de feuille (permet un restore sélectif). |
| 2026-06-23 | Pilotage COM intensif d'Excel (Unprotect/écritures/Calculate enchaînés) : `-2147418111` « L'appel a été rejeté par l'appelé » (Excel busy) et « Range.Formula can not be set » (feuille re-verrouillée). | Excel busy en COM = transitoire : envelopper chaque appel sensible dans un **retry + pause** sur `-2147418111`/'rejected'. Déprotéger via `ws.Unprotect("")` et VÉRIFIER `ProtectContents=False` avant d'écrire. Pour exporter une image d'une feuille protégée (`CopyPicture`+`ChartObjects.Add`), **déprotéger d'abord** : `Protect DrawingObjects:=True` bloque l'ajout du ChartObject (-2146827284). |
| 2026-06-23 | ROADMAP : réutilisé par erreur `X47`/`X48` (déjà pris dans « 🔧 À faire ») pour des features livrées → collision (un même `Xnn` = 2 sujets). Corrigé en X49/X50. | Avant d'attribuer un `Xnn`, **grep `Xnn` dans TOUT le ROADMAP** (« à faire » ET « implémentées ») et prendre le prochain libre. Le ROADMAP est la mémoire du projet : une collision de numéros la rend ambiguë. |
| 2026-06-23 | `modDashboardKPI.surconso()` + `modGraphiques` lisaient la surconso E85 en `Suivi Carburant!J7`, qui contient « Conso E85 réf. (km/L) » (~15) ; la vraie surconso (%) est en `J8` (~0,23). Décalage → éco et CO2 du bandeau faussés (`essEq=litres/(1+15)`). | Avant de lire une cellule « paramètre » en VBA, VÉRIFIER son libellé/sens RÉEL dans la feuille (colonnes libellés font foi) : un réagencement (ligne ajoutée) décale les cellules sans toucher au code. Ajouter un **garde-fou de plausibilité** (surconso fraction `0<x≤1`, sinon défaut) pour qu'un futur décalage ne reproduise pas une valeur aberrante. |
| 2026-06-23 | Sélecteur véhicule sur Suivi Carburant : j'avais d'abord posé une **cellule de validation** (B3) — l'utilisateur voulait un **vrai segment (slicer)** identique à `slcVehicule`. De plus `SlicerCache.Slicers.Add(dest,…)` **IGNORE** les coords Top/Left/W/H passées (place à ~750,334 par défaut) ; `shape.Top=` peut lever -2146777998 et `slicer.Name=` être refusé par Excel. | Pour un dispositif de sélection sur une feuille, privilégier un **segment** cohérent avec l'existant (pas une cellule de validation). **Réutiliser le cache existant** pour synchroniser 2 onglets : `wb.SlicerCaches("Segment_Vehicule").Slicers.Add(autreFeuille,…)` → les 2 slicers partagent la sélection (et déclenchent `Workbook_SheetPivotTableUpdate`→`ApplyFiltersFromControls`→B5). Après `Add`, **repositionner via la shape** avec `IncrementLeft/IncrementTop` (plus robustes que `.Left=`/`.Top=`), avec retry tolérant -2146777998. Déprotéger la feuille (`Protect DrawingObjects:=True` bloque l'ajout/déplacement de shapes). |
| 2026-06-23 | `Tableau2` (Suivi Carburant) : je craignais que ce soit une requête PQ (→ colonne ajoutée écrasée au refresh). En réalité `SourceType=xlSrcRange` (tableau de **formules INDEX** miroitant `GS_Pleins`, qui est LE QueryTable PQ) ; étendu par VBA `ListRows.Add`. | Vérifier `lo.SourceType` (1=plage/formules, 3=PQ) AVANT de modifier un tableau. Sur un tableau de formules, une colonne ajoutée comme **formule de colonne du ListObject** se propage automatiquement aux nouvelles lignes, et les agrégats en **références structurées** `Tableau2[col]` suivent la hauteur variable (vérifié : éco passée de 176→192 € après un plein ajouté) → robustes. Une colonne « valeur » manuelle, elle, ne suivrait pas. |
| 2026-06-23 | Proposé au ROADMAP un « bug » X52 (nettoyage des formes dashboard `For Each…Delete` qui sauterait des éléments → doublons) sur la seule base de **doublons observés** pendant un test — SANS lire le code. En réalité `CleanupDashShapes` était **déjà** une boucle **à rebours** correcte ; et le test prouve **0 accumulation** sur 2 `RecreerDashboardComplet` (34 formes / 4 cartes stables). Les doublons venaient de MON test initial (events laissés ACTIFS → un `DebouncedRebuild` différé `OnTime` s'était superposé à mon rendu). | Avant de proposer/coder la correction d'un « bug » observé, **lire le code source réel** ET **reproduire en conditions contrôlées** (`Application.EnableEvents=False`) : un symptôme vu pendant un test instrumenté (events ON, double appel manuel) est souvent un **artefact du test**, pas un bug du code. Ne pas inscrire au ROADMAP une correction non vérifiée contre la source. |
| 2026-06-24 | Le coût du kit était lu par le code en `Suivi Carburant!B5` (vide) alors que la feuille le saisit/calcule en `B6` (libellé A6, `B12=MAX(0,B6-B11)`). `KitCost` masquait le bug par un repli `Find("kit")` non ancré qui matchait en réalité le **titre** `A2` (« …KIT ÉTHANOL ») → valeur figée au défaut 514,54 quel que soit B6. | Une cellule « paramètre » lue en VBA peut être DÉSALIGNÉE de la cellule réellement utilisée par la feuille (suite à un réagencement). La trouver via son **libellé** ET les **formules de la feuille** qui la consomment, pas via le mapping du code. Un repli `Find(motclé)` non ancré peut matcher un titre/autre libellé → l'ancrer (libellé exact + colonne) ou le supprimer dès que la cellule cible est connue. Prouver le fix par un test de sensibilité (changer la cellule → la sortie suit). |
| 2026-06-24 | Sessions interrompues/reprises à répétition : `GetActiveObject("Excel.Application")` échouait (`-2147221021`/inaccessible) alors qu'un process EXCEL existait (instance hors ROT, classeur fermé puis rouvert entre les reprises) ; et le fix X55 avait déjà été **codé sur disque** dans une passe précédente (risque de le refaire). | À la reprise : (1) `git diff` AVANT de recoder — un fix peut déjà être sur disque non commité → le **finaliser** (déployer/tester/committer), pas le refaire. (2) Excel instable : si `GetActiveObject` échoue, utiliser `win32com.Dispatch("Excel.Application")` (attache à l'instance vivante ou ouvre le classeur) + vérifier `ReadOnly`. (3) Pour savoir si un déploiement VBA a été **sauvé dans le `.xlsm`** sans ouvrir Excel : `python -m oletools.olevba --code <fichier>` puis grep le symbole attendu (ex. `MkRO`). |
| 2026-06-24 | Param « calculé » (formule) dans une synchro bidirectionnelle LWW : la surconso (J8 = formule) ne doit jamais être écrasée par un push app→Excel. | Pour une cellule-paramètre en **lecture seule** (formule) : ajouter un drapeau `cellRO` au descripteur + helper dédié (`MkRO`), garder `If … And Not cellRO Then WriteCell` côté sync, retirer le push côté Réglages, et s'appuyer sur le garde `HasFormula` existant. Le param reste **partagé en lecture** (Excel = source) sans risque d'écrasement. |
| 2026-06-25 | Déploiement COM d'un module VBA via `VBComponents.Add(1)` + `AddFromString` : Excel insère AUTOMATIQUEMENT `Option Explicit` en ligne 1 du nouveau module (option « Exiger la déclaration des variables » active). Comme le code source injecté commençait lui aussi par `Option Explicit` (après strip de la ligne `Attribute`), le module se retrouvait avec **deux `Option Explicit`** → erreur de compilation → **toutes** les macros du projet « indisponibles » (`SuiviVehicule_EnsureBoutons` injoignable, code -2146827284). | À l'injection COM d'un `.bas` : après `VBComponents.Add`, **vider le module** (`CodeModule.DeleteLines(1, CountOfLines)`) AVANT `AddFromString`, pour ne pas dupliquer le `Option Explicit` auto-inséré. Vérifier les 3 premières lignes après injection. Une seule erreur de compilation dans n'importe quel module rend TOUT le projet VBA non exécutable. |
